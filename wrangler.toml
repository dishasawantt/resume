# Cloudflare Pages Configuration
# Project: Disha Sawant Portfolio

name = "disha-portfolio"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# Pages-specific settings
# Note: For Cloudflare Pages, most config is done via dashboard
# This file is mainly for local development with wrangler

[vars]
# Non-secret environment variables
# Secrets (GROQ_API_KEY, SENDGRID_API_KEY) should be set in Cloudflare Dashboard

# Build settings (for reference)
# Build command: (leave empty - static site)
# Build output directory: . (root)
# Functions directory: functions/

# Local development
[dev]
port = 8788
